server:
  port: "8080"
  mode: "debug"

database:
  host: "localhost"
  port: 5432
  user: "crm"
  password: "crm_password"
  dbname: "crm_ticketon"
  sslmode: "disable"

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "change-me-in-production-use-32-char-secret"
  expiry_hours: 24

s3:
  bucket: "crm-recordings"
  region: "us-east-1"
  access_key: ""
  secret_key: ""
  endpoint: ""  # For MinIO: http://localhost:9000

telephony:
  provider: "asterisk"  # asterisk | twilio | zadarma | sipuni
  api_url: ""
  api_key: ""
  webhook_url: "http://your-server/api/v1/webhooks/telephony"

whatsapp:
  provider: "meta"  # meta | wazzup | chat-api
  api_url: "https://graph.facebook.com/v18.0"
  api_key: ""
  webhook_url: "http://your-server/api/v1/webhooks/whatsapp"
  phone_id: ""

ai:
  provider: "openai"  # openai | anthropic | local
  api_key: ""         # Set via AI_API_KEY env var
  model: "gpt-4o-mini"
  base_url: "https://api.openai.com/v1/chat/completions"
